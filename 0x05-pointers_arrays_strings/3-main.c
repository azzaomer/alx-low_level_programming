#include "main.h"
#include <stdio.h>

/**
 * main - checkthe code
 *
 * Return: Always0;
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
